[project]
name = "grad-cam"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.12.0",
    "diffusers>=0.36.0",
    "fastapi>=0.128.0",
    "scipy>=1.17.0",
    "setuptools>=80.10.1",
]
